version: '3.8'

services:
  lymphaclustering:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        - BUILD_ENV=production
    image: lymphaclustering:1.0.0
    container_name: lymphaclustering_app
    ports:
      - "8000:8000"
    volumes:
      - /mnt/dcs_ai/share/lymphaclustering/lymphaclustering/app.py:/app/lymphaclustering/app.py
      - /mnt/dcs_ai/share/lymphaclustering/models:/app/models
      - /mnt/dcs_ai/share/lymphaclustering/data:/app/data
      - /mnt/dcs_ai/share/lymphaclustering/db:/app/db
      - /mnt/dcs_ai/share/lymphaclustering/logs:/app/logs
      - /mnt/dcs_ai/share/lymphaclustering/.env:/app/.env
      - /etc/localtime:/etc/localtime:ro
    working_dir: /app
    environment:
      - PYTHONPATH=/app
      - PYTHONUNBUFFERED=1
    env_file:
      - ../.env  # Load environment variables from file
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/docs"]  # Adjust endpoint as needed
      start_period: 30s
      interval: 30s
      timeout: 10s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
